# WinFS_InventoryCSV_V0.4.config
# Lines starting with # or // are comments
# Key names are case-insensitive
# Mode (not case-sensitive) Files → only files / Folder → only folders / Both → both files and folders

ScanFolder=C:\Temp\Tools
OutputFolder=C:\Tools\Output
Mode=Files
